export interface msgModel {
    code?: number,
    object?: any,
    msg?: string
}

export interface user {
    uid?: number,
    phone?: number,
    password?: string,
    profileName?: string,
    gender?: string
}